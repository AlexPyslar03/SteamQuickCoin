exports.start = function (request, response){
    // Получает информацию о нике пользователя Steam и данные для платежа
    // Заносит в бд ожидаемый платёж
    // Возвращает токен платежа и интендификатор транзакции в внутрений системе
};